import { useMemo, useState, useEffect, type FormEvent } from 'react'
import { Link, useNavigate, useParams } from 'react-router'
import { ArrowLeft, CalendarDays, Check, CheckCircle2, FileImage, MapPin, PackageCheck, ShieldCheck, Truck, XCircle } from 'lucide-react'
import { Header } from '../App'
import { getMyBookings as getBookings, updateBookingStatus } from '../services/bookings'
import type { MockBooking } from '../types'

export default function RentalReturn() {
  const { id } = useParams(); const navigate = useNavigate(); const [booking, setBooking] = useState<MockBooking | null>(null); useEffect(() => { getBookings().then(b => setBooking(b.find(x => x.id === id) || null)).catch(console.error) }, [id]); const [checks, setChecks] = useState([false, false, false, false]); const [notes, setNotes] = useState(''); const [file, setFile] = useState<File | null>(null); const [done, setDone] = useState(false)
  const preview = useMemo(() => file ? URL.createObjectURL(file) : '', [file])
  if (!booking) return <div dir="rtl" className="min-h-screen bg-cream"><Header /><main className="mx-auto max-w-lg px-5 py-24 text-center"><XCircle className="mx-auto text-rose" size={38} /><h1 className="mt-4 text-2xl font-black text-ink">لم نجد هذا الحجز</h1><Link to="/my-bookings" className="mt-5 inline-block font-bold text-brand">العودة إلى حجوزاتي</Link></main></div>
  const submit = async (event: FormEvent) => { event.preventDefault(); if (checks.some(value => !value)) return; try { await updateBookingStatus(booking.id, 'return_pending'); setDone(true) } catch (e) { console.error(e) } }
  if (done) return <ReturnSuccess bookingId={booking.id} listingName={booking.listingName} onView={() => navigate(`/my-bookings/${booking.id}`)} />
  return <div dir="rtl" lang="ar" className="min-h-screen bg-cream"><Header /><main className="mx-auto max-w-5xl px-5 py-8 sm:py-11"><Link to={`/my-bookings/${booking.id}`} className="inline-flex items-center gap-1.5 text-sm font-bold text-brand hover:underline"><ArrowLeft size={17} /> العودة إلى تفاصيل الحجز</Link><div className="mt-6 grid gap-7 lg:grid-cols-[minmax(0,1fr)_19rem]"><section><div className="rounded-[1.8rem] border border-line bg-white p-6 sm:p-8"><p className="text-xs font-black tracking-[.14em] text-amber">إنهاء التأجير</p><h1 className="editorial-display mt-2 text-4xl leading-tight text-ink">لنُتمّ إرجاع العنصر بسهولة.</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-ink/60">راجع حالة العنصر قبل الإرجاع وأرسل طلبك للمالك. لن يكتمل التأجير إلا بعد تأكيد الاستلام.</p><form onSubmit={submit} className="mt-8 space-y-6"><section className="rounded-2xl bg-cream p-5"><p className="text-xs font-black tracking-[.12em] text-brand">١ · تحقق سريع</p><h2 className="mt-2 text-xl font-black text-ink">هل كل شيء جاهز للعودة؟</h2><div className="mt-5 space-y-3">{['العنصر بحالته المتفق عليها', 'الملحقات الأساسية مرفقة', 'لم يتعرض العنصر لضرر جديد', 'تم الاتفاق على وقت الإرجاع مع المالك'].map((label, index) => <label key={label} className={`flex cursor-pointer items-center gap-3 rounded-xl border p-4 text-sm font-bold transition ${checks[index] ? 'border-green/30 bg-green/5 text-green' : 'border-line bg-white text-ink/70 hover:border-brand'}`}><input checked={checks[index]} onChange={event => setChecks(current => current.map((value, position) => position === index ? event.target.checked : value))} type="checkbox" className="h-4 w-4 accent-[#3d8c6c]" />{label}{checks[index] && <Check size={17} className="mr-auto" />}</label>)}</div></section><section className="rounded-2xl border border-line p-5"><p className="text-xs font-black tracking-[.12em] text-brand">٢ · ملاحظات وصور</p><label className="mt-4 block text-sm font-bold text-ink">ملاحظة للمالك <span className="font-medium text-muted">(اختياري)</span><textarea value={notes} onChange={event => setNotes(event.target.value)} rows={3} placeholder="مثال: سأصل في الموعد المتفق عليه، والملحقات كلها موجودة." className="mt-2 w-full resize-none rounded-xl border border-line px-4 py-3 text-sm font-normal outline-none focus:border-brand focus:ring-2 focus:ring-brand/10" /></label><div className="mt-5">{file && preview ? <div className="flex items-center gap-3 rounded-xl bg-brand-soft p-3"><img src={preview} alt="معاينة صورة العنصر" className="h-14 w-14 rounded-lg object-cover" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-ink">{file.name}</p><p className="mt-1 text-xs text-green">صورة مرفقة محليًا</p></div><button type="button" onClick={() => setFile(null)} className="text-xs font-bold text-rose">حذف</button></div> : <label className="flex cursor-pointer items-center justify-center gap-2 rounded-xl border-2 border-dashed border-line px-4 py-5 text-sm font-bold text-brand transition hover:border-brand hover:bg-brand-soft"><FileImage size={19} /> أضف صورة لحالة العنصر <input type="file" accept="image/*" onChange={event => setFile(event.target.files?.[0] ?? null)} className="sr-only" /></label>}</div></section><section className="rounded-2xl border border-amber/30 bg-amber/10 p-5"><div className="flex gap-3"><ShieldCheck className="mt-0.5 shrink-0 text-[#9c6514]" size={20} /><p className="text-sm leading-7 text-ink/70">سيصل طلب الإرجاع إلى المالك. احتفظ بمحادثة التأجير لتأكيد مكان ووقت التسليم النهائي.</p></div><button disabled={checks.some(value => !value)} className="mt-5 flex h-14 w-full items-center justify-center gap-2 rounded-xl bg-amber text-sm font-black text-brand disabled:cursor-not-allowed disabled:opacity-45"><PackageCheck size={18} /> إرسال طلب الإرجاع</button></section></form></div></section><aside className="space-y-4"><section className="overflow-hidden rounded-[1.5rem] border border-line bg-white"><img src={booking.listingImage} alt={booking.listingName} className="h-40 w-full bg-brand-soft object-cover" /><div className="p-5"><p className="text-xs font-black tracking-[.12em] text-brand">العنصر المستأجر</p><h2 className="mt-2 text-lg font-black text-ink">{booking.listingName}</h2><div className="mt-5 space-y-3 border-t border-line pt-4 text-sm text-ink/60"><p className="flex items-center gap-2"><CalendarDays size={17} className="text-brand" /> {booking.start} — {booking.end}</p><p className="flex items-center gap-2"><MapPin size={17} className="text-brand" /> {booking.delivery ? 'توصيل إلى موقعك' : 'استلام شخصي'}</p><p className="flex items-center gap-2"><Truck size={17} className="text-brand" /> الإرجاع مع {booking.ownerName}</p></div></div></section></aside></div></main></div>
}

function ReturnSuccess({ bookingId, listingName, onView }: { bookingId: string; listingName: string; onView: () => void }) { return <div dir="rtl" lang="ar" className="min-h-screen bg-cream"><Header /><main className="mx-auto flex min-h-[calc(100vh-90px)] max-w-xl items-center px-5 py-12"><section className="w-full rounded-[2rem] border border-line bg-white p-8 text-center shadow-[0_28px_70px_-44px_rgba(91,46,95,.35)]"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-green/10 text-green"><CheckCircle2 size={38} /></div><p className="mt-7 text-xs font-black tracking-[.14em] text-green">تم إرسال طلب الإرجاع</p><h1 className="editorial-display mt-3 text-4xl leading-tight text-ink">بقي تأكيد المالك فقط.</h1><p className="mx-auto mt-4 max-w-md text-sm leading-7 text-ink/60">تم تسجيل طلب إرجاع <strong>{listingName}</strong>. تواصل مع المالك لتأكيد الوقت والمكان.</p><div className="mt-8 rounded-2xl bg-cream p-4 text-right text-sm"><p className="font-bold text-ink">رقم الحجز</p><p className="mt-1 font-mono text-brand">{bookingId}</p></div><button onClick={onView} className="mt-7 rounded-xl bg-brand px-6 py-3.5 text-sm font-black text-cream">العودة إلى تفاصيل الحجز</button></section></main></div> }
